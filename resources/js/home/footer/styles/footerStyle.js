import { makeStyles } from "@material-ui/core";

const footerStyle = makeStyles((theme)=>({
    footerContainer:{
        padding:10,
        backgroundColor:'#0e0f3e',
        color:'white',
        display:'flex',
        paddingLeft:150,
        paddingRight:150,
        marginTop:50
    }
}))

export default footerStyle